import { redirect } from "next/navigation";

/** Scenes and recognition pieces retired — Observatory is living questions. */
export default function ObservatoryProtoRedirect() {
  redirect("/observatory");
}
